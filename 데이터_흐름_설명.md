# 지도와 통계 화면의 데이터 흐름 설명

## 1. 지도 화면 (CafeFindScreen) 데이터 흐름

### Q1: 지도 화면에서 카페 데이터는 어떻게 가져오나요?

**A:** 다음 순서로 데이터를 가져옵니다:

#### 1단계: 위치 정보 획득
```
[프론트엔드] useCurrentPosition Hook
  ↓
  - Android: PermissionsAndroid로 위치 권한 요청
  - iOS: Geolocation.requestAuthorization()로 위치 권한 요청
  ↓
  - Geolocation.watchPosition()으로 실시간 위치 감시
  ↓
  반환: { lat: number, lng: number } 좌표 정보
```

#### 2단계: 지도 중심 좌표 결정
```
[프론트엔드] CafeFindScreen.tsx
  ↓
  - GPS 좌표가 있으면: mapCenter 또는 GPS 좌표 사용
  - GPS 좌표가 없으면: DEFAULT_COORDS (서울시청: 37.5665, 126.978) 사용
  ↓
  - 지도 이동 시 handleCenterChanged() 호출 (디바운스 500ms 적용)
  ↓
  - searchCoords 결정 → API 호출에 사용
```

#### 3단계: API 호출 체인
```
[프론트엔드] useNearCafe Hook (React Query 사용)
  ↓
  - queryKey: ['cafes', coords]
  - enabled: coords가 있을 때만 실행
  - staleTime: 60초 (1분간 캐시 유지)
  ↓
  [프론트엔드] api/cafes.ts → fetchCafes()
  ↓
  HTTP GET 요청: /api/cafes?lat={lat}&lng={lng}&radius=1000
  ↓
  [백엔드] http.ts → BASE_URL = http://10.0.2.2:8080 (Android) 또는 http://localhost:8080 (iOS)
```

#### 4단계: 백엔드 처리
```
[백엔드] MapCafeController.java
  ↓
  @GetMapping("/api/cafes")
  - RequestParam: lat, lng, radius (기본값 1000m)
  ↓
  MapCafeService.findNearby() 호출
  ↓
  [백엔드] MapCafeService.java
  ↓
  1. KakaoClient.searchCafes() → 카카오 로컬 API 호출
     - 카카오 API 키는 application.yml에서 ${KAKAO_REST_API_KEY}로 설정
     - 외부 API: https://dapi.kakao.com
  ↓
  2. 응답 데이터 변환
     - KakaoPlacesResponseDTO → MapCafeResponseDTO
     - 좌표 파싱 (문자열 → 숫자)
     - 거리 계산: GeoDistance.distance() 또는 카카오 API 응답의 distance 필드
  ↓
  3. 거리순 정렬 (가까운 순서대로)
  ↓
  반환: List<MapCafeResponseDTO>
    {
      id: number (Kakao place_id),
      name: string,
      address: string,
      lat: number,
      lng: number,
      distance: number (미터 단위)
    }
```

#### 5단계: 프론트엔드 표시
```
[프론트엔드] CafeFindScreen.tsx
  ↓
  - useNearCafe()에서 반환된 cafes 배열 사용
  ↓
  1. KakaoMap 컴포넌트에 전달
     - userCoords: 표시할 중심 좌표
     - cafes: 마커로 표시할 카페 배열
     - onMarkerPress: 마커 클릭 시 선택 ID 저장
     - onCenterChanged: 지도 이동 시 새로운 좌표로 재검색
  ↓
  2. CafeListItem으로 목록 표시
     - 각 카페를 카드 형태로 렌더링
     - 거리 정보 표시 (예: "150m")
     - 선택된 카페는 'pin' 아이콘, 나머지는 'cafe' 아이콘
```

---

### Q2: 지도 화면에서 실시간 검색은 어떻게 작동하나요?

**A:** 디바운스(Debounce) 기법을 사용합니다:

```
사용자가 지도를 드래그하여 이동
  ↓
KakaoMap의 onCenterChanged 이벤트 발생
  ↓
handleCenterChanged() 호출
  ↓
이전 타이머가 있으면 clearTimeout()으로 취소
  ↓
새 타이머 설정: 500ms 후 실행
  ↓
500ms 동안 지도 이동이 없으면 → setMapCenter() 호출
  ↓
mapCenter 상태 변경 → useEffect 트리거
  ↓
searchCoords 변경 → useNearCafe Hook 재실행
  ↓
React Query가 자동으로 API 재호출
```

**효과**: 사용자가 지도를 빠르게 움직여도 500ms 후에 한 번만 API를 호출하여 불필요한 요청을 줄입니다.

---

## 2. 통계 화면 (StatisticsScreen) 데이터 흐름

### Q3: 통계 화면에서 어떤 데이터를 표시하나요?

**A:** 두 가지 주요 데이터를 표시합니다:

1. **월별 일별 카페인 섭취량 그래프** (DateChartPoint[])
2. **최근 마신 음료 목록** (Drink[])

---

### Q4: 월별 일별 카페인 섭취량 그래프 데이터는 어떻게 가져오나요?

**A:** 다음 순서로 데이터를 가져옵니다:

#### 1단계: 사용자 정보 확인
```
[프론트엔드] StatisticsScreen.tsx
  ↓
  getCurrentUser() → AsyncStorage에서 현재 로그인한 사용자 정보 가져오기
  ↓
  userId 확보
```

#### 2단계: 연도/월 결정
```
[프론트엔드] StatisticsScreen.tsx
  ↓
  1. 모든 Intake 데이터 로드 (fetchIntakes)
     - 초기 useEffect에서 한 번만 실행
     - 모든 섭취 기록을 가져와서 연도 추출
  ↓
  2. getYearAndMonth() 함수로 실제 데이터가 있는 연도 결정
     - 선택된 월(예: "3월")에 해당하는 데이터가 있는 연도 찾기
     - 여러 연도에 데이터가 있으면 가장 최근 연도 사용
     - 데이터가 없으면 현재 연도 사용
  ↓
  { year: 2025, month: 3 } 형태로 결정
```

#### 3단계: API 호출
```
[프론트엔드] api/statistics.ts → fetchMonthlyDateChart()
  ↓
  HTTP GET 요청: /api/statistics/monthly-date-chart?userId={userId}&year={year}&month={month}
  ↓
  [백엔드] http.ts → BASE_URL + 경로
```

#### 4단계: 백엔드 처리
```
[백엔드] StatisticsController.java
  ↓
  @GetMapping("/api/statistics/monthly-date-chart")
  - @RequestParam: userId, year, month
  ↓
  1. 해당 월의 시작일/종료일 계산
     - startDate: year년 month월 1일 00:00:00
     - endDate: year년 month월 마지막일 23:59:59
  ↓
  2. IntakeRepository.findByUser_IdAndConsumedAtBetween() 호출
     - 사용자 ID와 날짜 범위로 섭취 기록 조회
     - DB 쿼리: WHERE user_id = ? AND consumed_at BETWEEN ? AND ?
  ↓
  3. 날짜별로 그룹화 (Java Stream API)
     - LocalDate 기준으로 그룹핑
     - 각 날짜의 모든 Intake를 List로 묶음
  ↓
  4. DateChartPointDTO 변환
     - 각 날짜별로:
       * date: "1일", "2일", ... (일자만 추출)
       * value: 해당 일의 모든 Intake의 caffeineMg 합계
       * target: 400.0 (고정값, 일일 권장 카페인량)
  ↓
  5. 날짜순 정렬 (1일, 2일, 3일, ... 순서)
  ↓
  반환: List<DateChartPointDTO>
    {
      date: string,      // "1일", "2일", ...
      value: number,     // 해당 일의 총 카페인량 (mg)
      target: number     // 400 (일일 권장량)
    }
```

#### 5단계: 그래프 표시
```
[프론트엔드] StatisticsScreen.tsx
  ↓
  dateChartData 상태에 저장
  ↓
  StatisticsDateLineChart 컴포넌트에 전달
  ↓
  react-native-gifted-charts의 LineChart로 시각화
```

---

### Q5: 최근 마신 음료 목록 데이터는 어떻게 가져오나요?

**A:** 다음 순서로 데이터를 가져옵니다:

#### 1단계: 월별 Intake 데이터 조회
```
[프론트엔드] StatisticsScreen.tsx
  ↓
  fetchIntakesByMonth(userId, year, month) 호출
  ↓
  HTTP GET 요청: /api/intakes/monthly?userId={userId}&year={year}&month={month}
```

#### 2단계: 백엔드 처리
```
[백엔드] IntakeController.java
  ↓
  @GetMapping("/api/intakes/monthly")
  - @RequestParam: userId, year, month
  ↓
  1. 해당 월의 시작일/종료일 계산
     - startDate: year년 month월 1일 00:00:00
     - endDate: year년 month월 마지막일 23:59:59
  ↓
  2. IntakeRepository.findByUser_IdAndConsumedAtBetween() 호출
     - 사용자 ID와 날짜 범위로 섭취 기록 조회
  ↓
  3. IntakeDTO로 변환
     {
       id: Long,
       userId: Long,
       beverageId: Long,
       beverageName: string,
       volumeMl: number,
       caffeineMg: number,
       consumedAt: LocalDateTime,
       note: string
     }
  ↓
  4. 최신순 정렬 (consumedAt 내림차순)
  ↓
  반환: List<IntakeDTO>
```

#### 3단계: 프론트엔드 변환 및 필터링
```
[프론트엔드] StatisticsScreen.tsx
  ↓
  백엔드 API 실패 시 Fallback: allIntakes에서 프론트엔드 필터링
  ↓
  최대 10개만 선택 (slice(0, 10))
  ↓
  convertIntakeToDrink() 함수로 변환
  - note 필드 또는 beverageName에서 음료 이름 추출
  - 브랜드와 이름 분리 (예: "스타벅스 아이스 아메리카노" → brand: "스타벅스", name: "아이스 아메리카노")
  ↓
  Drink 타입으로 변환
  {
    id: string,
    brand: string,
    name: string,
    price: number,
    favorite: boolean
  }
  ↓
  recentDrinks 상태에 저장
```

#### 4단계: 목록 표시
```
[프론트엔드] StatisticsScreen.tsx
  ↓
  DrinkList 컴포넌트에 전달
  ↓
  각 Drink를 카드 형태로 렌더링
```

---

## 3. 전체 아키텍처 요약

### 데이터 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                        프론트엔드 (React Native)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  [지도 화면]                           [통계 화면]                │
│  CafeFindScreen.tsx                StatisticsScreen.tsx          │
│      │                                    │                      │
│      ├─ useCurrentPosition()             ├─ getCurrentUser()    │
│      │   (GPS 위치)                       │   (사용자 정보)       │
│      │                                    │                      │
│      └─ useNearCafe()                    ├─ fetchMonthlyDateChart()
│          (React Query)                   │   (그래프 데이터)      │
│          │                               │                      │
│          └─ api/cafes.ts                 └─ fetchIntakesByMonth()
│              fetchCafes()                    (음료 목록)         │
│                                                                   │
│      └─ api/intakes.ts              └─ api/statistics.ts        │
│                                                                   │
└──────────────────────┬───────────────────┬──────────────────────┘
                       │                   │
                       │ HTTP Request      │ HTTP Request
                       │ (GET /api/cafes)  │ (GET /api/statistics/...)
                       │                   │ (GET /api/intakes/...)
                       ▼                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                      백엔드 (Spring Boot)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  MapCafeController            StatisticsController               │
│      │                              │                            │
│      └─ MapCafeService              └─ IntakeRepository          │
│          │                                                      │
│          ├─ KakaoClient              IntakeController           │
│          │   (외부 API)              │                          │
│          │   │                       └─ IntakeRepository        │
│          │   └─ Kakao Local API                                 │
│          │       (카페 검색)                                      │
│          │                                                      │
│          └─ GeoDistance                                        │
│              (거리 계산)                                         │
│                                                                   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │      데이터베이스 (MySQL)     │
              │                              │
              │  - User 테이블              │
              │  - Intake 테이블            │
              │  - Beverage 테이블          │
              │  - Favorite 테이블          │
              │                              │
              └─────────────────────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │    외부 API (카카오 로컬)      │
              │                              │
              │  - 카페 검색 API             │
              │  - REST API Key 필요         │
              └─────────────────────────────┘
```

---

## 4. 주요 기술 스택 및 패턴

### 프론트엔드
- **상태 관리**: React Query (서버 상태), React useState/useEffect (로컬 상태)
- **HTTP 클라이언트**: Axios (http.ts에서 인스턴스 생성)
- **위치 서비스**: @react-native-community/geolocation
- **캐싱**: React Query의 staleTime 설정 (60초)

### 백엔드
- **프레임워크**: Spring Boot 3.5.7
- **데이터베이스**: MySQL (JPA/Hibernate)
- **외부 API 연동**: RestTemplate 또는 WebClient (KakaoClient)
- **트랜잭션**: @Transactional(readOnly = true) (조회 전용)

### 데이터 변환
- **Entity → DTO**: 백엔드에서 Java Record 사용 (IntakeDTO, MapCafeResponseDTO 등)
- **DTO → UI 타입**: 프론트엔드에서 변환 함수 사용 (convertIntakeToDrink 등)

---

## 5. 질의 응답 예시

### Q: 지도 화면에서 카페가 안 나와요. 어떻게 확인하나요?
**A:** 다음을 확인하세요:
1. 위치 권한이 허용되었는지 확인 (Android: 설정 → 앱 → 권한)
2. 백엔드 서버가 실행 중인지 확인 (http://10.0.2.2:8080)
3. 카카오 API 키가 올바르게 설정되었는지 확인 (application.yml의 KAKAO_REST_API_KEY)
4. 콘솔 로그 확인:
   - `[지도화면] GPS 위치:`
   - `[API] 카페 검색 요청:`
   - `[Controller] 카페 검색 요청:`

### Q: 통계 화면에 데이터가 안 나와요. 어떻게 확인하나요?
**A:** 다음을 확인하세요:
1. 로그인한 사용자가 있는지 확인 (getCurrentUser())
2. 해당 월에 Intake 데이터가 있는지 확인 (DB 직접 조회)
3. 백엔드 API 응답 확인:
   - `/api/intakes?userId={userId}` - 전체 섭취 기록
   - `/api/intakes/monthly?userId={userId}&year=2025&month=3` - 월별 섭취 기록
   - `/api/statistics/monthly-date-chart?userId={userId}&year=2025&month=3` - 그래프 데이터
4. 콘솔 로그 확인:
   - `[Statistics] All intakes loaded:`
   - `[Statistics] Chart data received:`

### Q: 지도에서 실시간 검색이 너무 느려요.
**A:** 디바운스 시간을 조정하세요:
- 현재: 500ms
- 더 빠르게: 300ms (더 많은 API 호출)
- 더 느리게: 1000ms (더 적은 API 호출)
- 파일: `CafeFindScreen.tsx`의 `handleCenterChanged` 함수

### Q: 통계 화면에서 연도 선택이 안 되는데?
**A:** 현재 구현에서는 자동으로 데이터가 있는 연도를 찾습니다. 수동 선택이 필요하면:
1. StatisticsHeader에 연도 선택 드롭다운 추가
2. selectedYear 상태 추가
3. fetchMonthlyDateChart 호출 시 선택된 연도 사용

---

## 6. API 엔드포인트 요약

### 지도 관련
- `GET /api/cafes?lat={lat}&lng={lng}&radius={radius}`
  - 반환: `MapCafeResponseDTO[]`
  - 설명: 지정된 좌표 반경 내의 카페 검색

### 통계 관련
- `GET /api/statistics/monthly-date-chart?userId={userId}&year={year}&month={month}`
  - 반환: `DateChartPointDTO[]`
  - 설명: 월별 일별 카페인 섭취량 그래프 데이터

- `GET /api/statistics/popular-drinks?userId={userId}&year={year}&month={month}`
  - 반환: `FrequentDrinkDTO[]`
  - 설명: 월별 자주 마시는 음료 목록 (현재 화면에서 미사용)

### 섭취 기록 관련
- `GET /api/intakes?userId={userId}`
  - 반환: `IntakeDTO[]`
  - 설명: 사용자의 전체 섭취 기록 (최신순)

- `GET /api/intakes/monthly?userId={userId}&year={year}&month={month}`
  - 반환: `IntakeDTO[]`
  - 설명: 특정 월의 섭취 기록 (최신순)








